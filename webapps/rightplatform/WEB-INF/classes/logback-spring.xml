<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="60 seconds" debug="false">

    <contextName>rightplatform</contextName>

    <property name="LOG_PATTERN" value="%d %-5level [%5.15t] %-30.40logger{39} %4.4line:%X{msgId}%msg%n"/>

    <property name="logDir" value="~/rightplatform/logs"/>
    <property name="appName" value="rightplatform"/>

    <!--输出到控制台 ConsoleAppender-->
    <appender name="consoleLog" class="ch.qos.logback.core.ConsoleAppender">
        <!--展示格式 layout-->
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>
    <appender name="infoLog" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <File>${logDir}/${appName}.log</File>
        <!--滚动策略，按照时间滚动 TimeBasedRollingPolicy-->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!--文件路径,定义了日志的切分方式——把每一天的日志归档到一个文件中,以防止日志填满整个磁盘空间-->
            <FileNamePattern>${logDir}/${appName}.%d{yyyy-MM-dd}.%i.log.gz</FileNamePattern>
            <!--只保留最近5天的日志-->
            <maxHistory>5</maxHistory>
            <maxFileSize>5GB</maxFileSize>
        </rollingPolicy>

        <encoder>
            <charset>UTF-8</charset>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <logger name="org.springframework" level="INFO" />
    <logger name="org.mybatis" level="INFO" />
    <logger name="com.telecom" level="INFO" />
    <logger name="com.chinatelecom" level="INFO" />

    <!--指定最基础的日志输出级别-->
    <root level="INFO">
        <!--appender将会添加到这个loger-->
        <appender-ref ref="consoleLog"/>
        <appender-ref ref="infoLog"/>
    </root>

</configuration>